"""ManagerWorker — placeholder for manager operations (most are UI-only)."""

from gui.workers.base_worker import BaseWorker


class ManagerWorker(BaseWorker):
    """Lightweight worker for manager operations."""
    pass
